<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <!-- 毎朝の「今日の復習」通知（Android 13+ は実行時にも許可が要る） -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
      android:name=".MainApplication"
      android:label="@string/app_name"
      android:icon="@mipmap/ic_launcher"
      android:roundIcon="@mipmap/ic_launcher_round"
      android:allowBackup="false"
      android:theme="@style/AppTheme"
      android:usesCleartextTraffic="${usesCleartextTraffic}"
      android:supportsRtl="true">
      <activity
        android:name=".MainActivity"
        android:label="@string/app_name"
        android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
        android:launchMode="singleTask"
        android:windowSoftInputMode="adjustResize"
        android:exported="true">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>

        <!-- 共有メニュー（他アプリの「共有」→ 学習トラッカー） -->
        <intent-filter>
            <action android:name="android.intent.action.SEND" />
            <category android:name="android.intent.category.DEFAULT" />
            <data android:mimeType="text/plain" />
        </intent-filter>

        <!-- テキスト選択メニュー（文字を選択 → メニューに 学習トラッカー） -->
        <intent-filter>
            <action android:name="android.intent.action.PROCESS_TEXT" />
            <category android:name="android.intent.category.DEFAULT" />
            <data android:mimeType="text/plain" />
        </intent-filter>
      </activity>

      <!-- NotionPlus 専用ランチャーアイコン（別名で MainActivity を起動）
           タップすると intent の component が .NotionPlusAlias になり、
           ネイティブ(LaunchRoute) → JS が NotionPlus タブを最初に開く -->
      <activity-alias
        android:name=".NotionPlusAlias"
        android:targetActivity=".MainActivity"
        android:label="@string/app_name_notionplus"
        android:icon="@mipmap/ic_launcher_np"
        android:roundIcon="@mipmap/ic_launcher_np_round"
        android:exported="true">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
      </activity-alias>

      <!-- 通知のチャンネル既定値。サーバーが channel_id を付け忘れてもここへ落ちる
           （チャンネル自体は MainApplication.createReviewChannel が作る）
           🔥 tools:replace が要る。@react-native-firebase/messaging が同じ meta-data を
              空文字で宣言しているため、付けないとマニフェストの合成で必ずビルドが落ちる。 -->
      <meta-data
        android:name="com.google.firebase.messaging.default_notification_channel_id"
        android:value="study_review"
        tools:replace="android:value" />

      <!-- 通知バーの小アイコンと差し色。
           🔥 指定しないとランチャーアイコンが使われ、Androidが色を潰して「灰色の四角」になる。
              指す先は必ず「白のシルエット＋透明」の drawable（ic_notification.xml）。 -->
      <meta-data
        android:name="com.google.firebase.messaging.default_notification_icon"
        android:resource="@drawable/ic_notification"
        tools:replace="android:resource" />
      <meta-data
        android:name="com.google.firebase.messaging.default_notification_color"
        android:resource="@color/notification_color"
        tools:replace="android:resource" />

      <!-- APK インストール用 FileProvider -->
      <provider
        android:name="androidx.core.content.FileProvider"
        android:authorities="${applicationId}.fileprovider"
        android:exported="false"
        android:grantUriPermissions="true">
        <meta-data
          android:name="android.support.FILE_PROVIDER_PATHS"
          android:resource="@xml/file_provider_paths" />
      </provider>
    </application>
</manifest>
